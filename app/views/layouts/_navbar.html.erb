<header>
  <nav class="navbar navbar-expand-lg border-bottom shadow-sm">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <i class="bi bi-box"></i>
        neumann</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mb-2 mb-lg-0">
          <li>
            <%= link_to "Home", root_path, class: "nav-link active" %>
          </li>
          <li>
            <%= link_to "Library", root_path, class: "nav-link active" %>
          </li>
          <li>
            <%= link_to "Requests", rental_requests_url, class: "nav-link active" %>
          </li>
          <li>
            <%= link_to "Games", games_path, class: "nav-link active" %>
          </li>
          <% if user_signed_in? && current_user.has_request_open?%>
            <% request = RentalRequest.find(current_user.rental_request_id) %>
            <li>
            <%= link_to "Opened request ##{request.id}", request, class: "nav-link active" %>
            </li>
          <% end %>
        </ul>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item dropdown">
            <% if user_signed_in? %>
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <%= current_user.name %>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user, class: "dropdown-item" %></li>
                <li><a class="dropdown-item" href="#">Rentals</a></li>
                <li><a class="dropdown-item" href="#">My games</a></li>
                <li>
                  <hr class="dropdown-divider">
                </li>
                <li><%= link_to "Sign out", logout_path, class: "dropdown-item" %></li>
              </ul>
            <% else %>
              <%= link_to "Log in", new_user_session_path, class: "nav-link" %>
            <% end %>
          </li>
        </ul>
        <%= search_form_for @q, class: "d-flex ms-2" do |f| %>
          <%= f.search_field :title_cont, class: "form-control me-2" %>
          <%= f.button class: "btn btn-outline-success" do %>
            <i class="bi bi-search"></i>
          <% end %>
        <% end %>
<!--        <form class="d-flex ms-2" role="search">-->
<!--          <input class="form-control me-2" type="search" placeholder="Search games" aria-label="Search">-->
<!--          <button class="btn btn-outline-success" type="submit">-->
<!--        </form>-->
      </div>
    </div>
  </nav>
</header>
