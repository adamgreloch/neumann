<p style="color: green"><%= notice %></p>

<h1>Rental requests</h1>
<p>The following list contains all active rental requests. Here, X
  <i class="bi bi-arrow-right"></i> Y means that the request submitter
  is interested in giving all games from set X in return for all games from set Y for a specified time period.
</p>

<div id="rental_requests">
  <ul class="list-group">
    <% @rental_requests.each do |rental_request| %>
      <li class="list-group-item d-flex">
        <div class="container-fluid">
          <div class="row align-items-center">
            <div class="col-auto">
              <%= link_to "Request \##{rental_request.id}", rental_request, class: "text-nowrap" %>
            </div>
            <div class="col-2">
              <small class="ms-1 text-muted">
                <%= l rental_request.rental_start %> -
                <%= l rental_request.rental_end %>
              </small>
            </div>
            <div class="col">
              <ul class="list-group list-group-horizontal">
                <% wanted = rental_request.wanted_per_requests %>
                <% if wanted.empty? %>
                  <li class="list-group-item">
                    <i class="bi bi-braces"></i>
                  </li>
                <% else %>
                  <% wanted.each do |entry| %>
                    <li class="list-group-item">
                      <%= link_to entry.game.title, entry.game %>
                    </li>
                  <% end %>
                <% end %>
                <li class="list-group-item">
                  <i class="bi bi-arrow-right"></i>
                </li>
                <% offered = rental_request.offered_per_requests %>
                <% if offered.empty? %>
                  <li class="list-group-item">
                    <i class="bi bi-braces"></i>
                  </li>
                <% else %>
                  <% offered.each do |entry| %>
                    <li class="list-group-item">
                      <%= link_to entry.game.title, entry.game %>
                    </li>
                  <% end %>
                <% end %>
              </ul>
            </div>
            <div class="col-1">
              <%= link_to "Accept", root_url, type: 'button', class: "btn btn-outline-primary" %>
            </div>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
</div>
