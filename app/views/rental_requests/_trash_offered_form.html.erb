<% if user_signed_in? && current_user.has_request_open? %>
  <%= button_to remove_offered_rental_request_path(@users_request), class: "btn btn-outline-primary",
                params: {game_id: game.id},
                method: :put do %>
    <i class="bi bi-x-lg"></i>
  <% end %>
<% end %>
