<%= bootstrap_form_with(model: user) do |f| %>
  <%= f.alert_message "Please fix the errors below." %>

  <%= f.text_field :deposit_paid %>
  <%= f.text_field :deposit_deducted %>

  <%= f.submit %>
<% end %>
