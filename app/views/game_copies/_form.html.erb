<%= bootstrap_form_with(model: game_copy, layout: :horizontal) do |f| %>

  <% if game_copy.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(game_copy.errors.count, "error") %> prohibited this game_copy from being saved:</h2>

      <ul>
        <% game_copy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :title %>
  <%= f.number_field :bgg_id %>
  <%= f.text_area :description %>
  <%= f.text_field :release_year %>
  <%= f.text_field :bgg_rating %>
  <%= f.text_field :bgg_rated_count %>
  <%= f.text_field :bgg_ranking %>
  <%= f.submit %>

<% end %>
